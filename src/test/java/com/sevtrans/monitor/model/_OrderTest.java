package com.sevtrans.monitor.model;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.TestInstance;
import org.junit.jupiter.api.TestInstance.Lifecycle;

import java.util.Date;
import java.util.HashSet;
import java.util.Set;

@TestInstance(Lifecycle.PER_CLASS)
public class _OrderTest {

    @SuppressWarnings("unchecked")
    @Test
    public void name() {
/*
        XStream xstream = new XStream();
        xstream.alias("product", _OrderDetail.class);
        xstream.alias("Order", _Order.class);
        _Order order =new _Order();
        order.getOrderDetail();
        order.setContractor("contractor");
        order.setCustomer("customer");
        order.setDriver("driver");
        order.setLicensePlate("license");
        order.setManager("manager");
        order.setOrderDate(new Date());
        order.setOrderNo("orderNo");
        order.setOrderType("orderType");
        order.setPlannedDate(new Date());
        order.setTypeOfDelivery("TypeOfDelivery");
        
        order.setVn(21);//???
                @SuppressWarnings("unchecked")
        Set<_OrderDetail> dtl=new HashSet();
        _OrderDetail d1 = new _OrderDetail();
        d1.setQty(1.0);
        d1.setArticle("art1");
        d1.setLineNo(1);
        d1.setMark2("mark2 1");
		dtl.add(d1);
        _OrderDetail d2 = new _OrderDetail();
        d2.setQty(1.0);
        d2.setArticle("art2");
        d2.setLineNo(2);
        d2.setMark2("mark2 2");
		dtl.add(d2);
		order.setOrderDetail(dtl);
        String out=xstream.toXML(order);
        System.out.println(out);
*/

    }

}
